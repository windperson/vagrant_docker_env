web:
  build: ./nginx
  links:
    - phpfpm
  volumes_from:
    - phpfpm
  ports:
    - "80:80"

phpfpm:
  build: ./phpfpm
  volumes:
   - phpfpm/src:/var/www/html
  links:
   - redis

redis:
  image: redis
